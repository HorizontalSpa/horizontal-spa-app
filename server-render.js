// Точка входа для Render.com (Start Command: node server-render.js).
// Раньше этот файл был отдельным упрощённым сервером и НЕ поднимал API и БД —
// именно поэтому задеплоенное приложение не работало с бэкендом.
// Теперь используется единый сервер из server.js.
require('./server');
